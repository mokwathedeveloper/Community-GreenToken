# Missing Sidebar Asset — Action Required

**File needed:** `eco_friendly_blockchain_impact_tracker.png`

This image was deleted from the repository (previously stored as `eco_friendly_blockchain_impact_tracker.wemp` — note the incorrect `.wemp` extension).

## What to do
Re-source or recreate this image with the following requirements:
- **Content:** Community GreenToken impact tracker illustration (eco / blockchain theme)
- **Format:** PNG with **transparent background (RGBA)**
- **Dimensions:** 160 × 160 px (square, suitable for sidebar bottom slot)
- **Style:** Consistent with `green_earth_and_sprout.png` in the donationsidebar folder

## Where it is used
`implementation/sidebar_image_md.md` — `SidebarBottomImage.jsx` component

## Temporary workaround
Until this asset is re-sourced, use `green_earth_and_sprout.png` from
`assets/image/donationsidebar/` as a stand-in.
